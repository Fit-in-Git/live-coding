import Navigation from "../components/Navigation";

const Header = () => {
  return (
    <header style={{background: "#dbeafe", padding: "15px"}}>
      <Navigation />
    </header>
  );
};

export default Header;
